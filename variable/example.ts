const myName: string = 'Hwang in seon';
const myAge: number = 24;
const placeOfBirth: string = 'In-cheon';

const myFavoriteMusic: { title: string, singer: string } = {
  title: 'first love',
  singer: 'Baek Ah'
}

const myProject: { member: string[], days: number, started: boolean } = {
  member: ['hwang', 'moon'],
  days: 30,
  started: true,
}